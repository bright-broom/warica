# 確認済み状態 — STATUS

AI・開発者は[AGENTS.md](../AGENTS.md)から本書、次に[BACKLOG.md](BACKLOG.md)を読む。本書は**何が確認済みで、何が未確認かの正本**。課題の状態・完了条件はBACKLOG、製品の使い方は[README](../README.md)で管理する。

## この記録の時点

- 更新：2026-09-14（支払いリフレッシュ改善）。
- 棚卸し対象：`191f725746d925e1a6ededa7d225cdf45b0488f8`。
- 引き継ぎ開始時のmain：`0354e5bf3052113ebfccdc61f38a4df36596427c`。棚卸し対象とファイル内容が同じことを確認。コミットが進んだだけでWAR-001〜003が修正されたと判断しない。
- 今回の実装・検証対象：`38416b49adf36f7cbd9a5032615e43245ba0d4d8`。右上のリフレッシュを支払いリセットへ変更（WAR-009）。ローカル検証済み、本番反映・実機確認は未確認。棚卸しのWAR-001〜003を解決した変更ではない。
- 以下は確認日時付きのスナップショット。着手時・公開時は現行のコード、PR、CI、デプロイを再確認する。

## 実装済み

| 領域         | 確認済みの状態                                                                        |
| ------------ | ------------------------------------------------------------------------------------- |
| 初回入力     | イベント「みんなでごはん」、メンバーA・B。保存済みイベントは初期値で上書きしない      |
| 支払い・精算 | 対象者別配分、円単位の端数処理、追加・編集・削除、20件ずつの履歴・全件検索            |
| 共有         | 全送金一覧の一括コピー、相手別コピー、クリップボード失敗時の代替テキスト              |
| PayPay       | 発行済み請求リンクの登録・保持・コピー・受け渡し。精算内容が変わるとリンクを無効化    |
| 保存         | ブラウザ内の同期保存、正常な旧保存内容のバックアップ、形式検証・旧データの読み込み    |
| 下書き       | 支払いの新規入力と編集を別々に保持し、同じタブ内の画面移動・再読み込みで復元          |
| 復旧・競合   | 保存失敗時に入力を保持、再試行・JSON復旧、別タブ更新の検知・確認後の切り替え          |
| UI           | スマホはページ内矢印、PCはサイドバーも使用。共通Provider・Tailwind・shadcn/ui・Lucide |
| ドキュメント | READMEの図解8点・実画面3点、デザイン管理・性能・公開検証の記録                        |

「実装済み」は境界条件まで無欠陥という意味ではない。金額入力・過長下書き・保存容量上限に、[再現済みの未修正課題](BACKLOG.md#war-001)がある。

## 維持する仕様・ユーザーの決定

- 文字を最小限にし、意味が伝わる操作はアイコン化。必要な操作名・ツールチップ・アクセシビリティは保つ。
- スマホのタップ操作は右側に集約。下部固定の3タブは削除済みで、復活させる前提にしない。
- ポップなデザインに統一。基本色はメイン`#193B3A`、サブ`#F4F8F7`、アクセント`#69DCCB`。ミントのグラデーションはアクセントに使い、本文フォントのグラデーション化はしない。
- 旧デザイン・デザイン切り替え・3点メニュー・保存チェック表示・「新しく始める」は削除済み。
- 2026-09-14の最新指示により、右上のリフレッシュは確認後に支払い履歴・精算結果・下書き・PayPayリンクを消す操作へ変更。イベント名・メンバーは残して次の金額入力へ進む。ブラウザ通常の再読み込みはデータを保持する。
- 「バックアップ」「読み込む」は現在は保存トラブル時の復旧パネルに表示。正常時の持ち出し導線はWAR-010で判断待ち。支払いを消して再利用する導線はWAR-009で実装・ローカル検証済み。
- PayPayは請求リンク方式。金額を指定する個人間送金API、送金完了の判定、実送金の代行は未実装。現在の方式に不足するAPIキーがあるわけではない。
- ログイン・共同編集・クラウド同期・イベント一覧は未提供。未提供だけを理由に追加せず、依頼と仕様判断の範囲で扱う。

## 検証記録

| 確認対象                     | 結果・範囲                                                                                        | 限界・参照                                                                                                                                                      |
| ---------------------------- | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-14のローカル総合検証 | 単体35件成功、E2E110件成功・対象外1件。型・静的検査・ビルド・書式・依存監査も成功                 | 本体は`9aff4de9`、テスト修正は`2e16bb7`（mainへ#22で統合）。その後のREADME更新は本体変更なし                                                                    |
| ブラウザ構成                 | Chromium PC 1440×1100、Chromiumモバイル320×800、WebKitモバイル390×844                             | 対象外1件はスマホ専用レイアウト検査のPC実行。実機Safari/Chromeの証拠ではない                                                                                    |
| 追加の表示・操作確認         | 320×568、390×844、844×390、768×1024、1023×768、1024×768、1440×1100で主要3画面・登録後の再読み込み | JavaScript例外・HTTP 5xx・横方向のはみ出しを検査。実機キーボード未検証                                                                                          |
| 履歴検索テストの修正         | 遷移先の表示を待つよう修正し、3環境×5回成功。全体も単独実行で成功                                 | 同時テストの出力先競合が起きた中間実行は成功記録に含めていない。次回は`--output`を分ける                                                                        |
| アクセシビリティ             | axe、操作名、キーボード・フォーカス、タップ領域を自動検査                                         | VoiceOver/TalkBack、実機拡大等の手動検査は未実施。完全適合の証明ではない                                                                                        |
| 2026-09-14の境界条件調査     | 通常テスト外の3系統で問題を再現                                                                   | [観測結果JSON](verification/2026-09-14-boundary-cases.json)。WAR-001〜003は未修正                                                                               |
| CIの最終成功確認             | `0354e5b`の[CI成功](https://github.com/bright-broom/warica/actions/runs/34789281256)              | 08:24に完了・成功を再確認。先行する`191f725`の[CI](https://github.com/bright-broom/warica/actions/runs/34787778612)も成功。この文書追加コミットのCI結果ではない |
| Production                   | `0354e5b`のdeployment `6427662684`成功を08:24に確認。公開URLのHTTP 200は同日の棚卸し時に確認      | [公開サイト](https://warica.vercel.app)の最新全操作・配信ビルドとの厳密な対応は未確認。デプロイ成功だけで操作確認済みにしない                                   |
| README                       | 8つのMermaid図を描画、相対リンク・画像26件と目次アンカー、書式を確認                              | 2026-09-14の更新時点。今後の編集についてはCI未整備（WAR-011）                                                                                                   |

テストの件数と成功は日付・対象付きの履歴。新しく見つけた境界条件を否定する材料にしない。実行方法は[READMEのテスト手順](../README.md#テスト)、構成は[playwright.config.ts](../playwright.config.ts)と[CI定義](../.github/workflows/ci.yml)を参照する。

### 支払いリフレッシュ改善の検証（2026-09-14）

`38416b4`で単体35件、全E2E125件成功・スマホ専用条件のPC実行1件は対象外。3画面からのリセット、キャンセル、グループ保持、次の登録、通常再読み込み、保存失敗、確認中の別タブ更新、下書き保存失敗後の再読み込み、確認ダイアログのaxe検査を実施した。[検証記録](verification/2026-09-14-payment-reset.md)を参照。上表の旧110件は変更前の履歴として残す。

## 未確認・仕様判断待ち

- 最新公開環境の全操作、実機iPhone/Android、PayPay実アプリの受け渡し、大量データ時の実機応答速度はWAR-005〜008。
- 厳密な同時保存の保護はWAR-004。通常の別タブ更新検知と同時書き込みの排他制御を混同しない。
- WAR-009はリフレッシュからの支払いリセットとして実装・ローカル検証済み。正常時の持ち出し・復元はWAR-010で判断待ち。イベント名・メンバーを含む全消去や複数イベント管理を追加したわけではない。
- 継続的な文書検査と依存監査はWAR-011・012。GitHub側の依存関係通知設定は未確認。
- 現仕様でログイン・クラウドDBの本番設定は不要。外部サービスの設定不足を確認したわけではない。

## 次の着手順

ユーザーから別の優先指示がなければ、まず[WAR-001](BACKLOG.md#war-001)・[WAR-002](BACKLOG.md#war-002)・[WAR-003](BACKLOG.md#war-003)を修正し、再現例を回帰テストへ追加する。その後に本番・実機・PayPayの確認、同時保存と性能、仕様判断・継続検査を進める。各項目の完了条件はBACKLOGを参照。

## 文書の更新方法

1. 確認済みの状態が変わったら、対象コミット・日時・環境・結果・限界を本書へ記録する。
2. 課題の状態はBACKLOGの一覧と詳細で更新する。未検証の実装を「完了」にしない。
3. 仕様・使い方が変わったらREADMEと必要な設計資料も更新する。完了した課題IDと過去の検証日を消さない。
4. [公開環境の検証記録](PRODUCTION_VERIFICATION.md)と[性能記録](STORAGE_PERFORMANCE.md)は、記載された日付・対象の履歴として読む。現状と矛盾があればコードと新しい証跡で確認し、違いを記録する。
