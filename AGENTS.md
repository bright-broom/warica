# WARICA — AI・開発者の作業入口

このファイルはリポジトリ全体に適用する共通の入口。ユーザーの最新の指示と実際のコード・検証結果を優先し、過去の文書を現在の事実や追加作業の承認として扱わない。

## 最初に読む

1. [docs/STATUS.md](docs/STATUS.md)：確認済みの状態、検証対象・日付・限界、維持する仕様。
2. [docs/BACKLOG.md](docs/BACKLOG.md)：固定ID付きの残課題、分類・状態・優先度・再現条件・完了条件。
3. [README.md](README.md)：使い方、図解、開発・テスト手順。UI変更時は[デザイン管理ガイド](docs/DESIGN_SYSTEM.md)も読む。

チャット履歴、個人のメモリ、Git管理外の`artifacts/`やローカル絶対パスに依存せず、上記から引き継げる状態を保つ。Claude・Gemini・Copilot向けの入口は参照だけにし、ルールや課題を複製しない。

## 作業開始時

- `git status --short --branch`で未保存変更と作業ブランチを確認する。他の人・AIの変更を上書きしない。
- 対象の実装を読み、関連するWAR-IDを確認する。文書の日付・対象コミットと現状が違う場合は再確認する。
- 公開や統合について報告・操作する前に、現在のPR・CI・デプロイ状況を確認する。ネットワークや認証の都合で確認できなければ「未確認」と記す。
- 自律的に次の作業を選ぶ依頼では、ユーザーの新しい優先指示がなければWAR-001〜003の修正を先に検討する。実機未検証・仕様判断待ちを、確認済みのバグと混同しない。

## 変更・検証時

- 登録済み台帳と支払い下書きの保存を保護する。検証には隔離したブラウザと合成データを使い、利用者のサイトデータを消さない。
- 削除済みの下部タブ、旧デザイン、3点メニュー、「新しく始める」を、欠落した機能だと判断して自動で復活させない。経緯と現在の仕様はSTATUSとBACKLOGで確認する。
- 修正の範囲に合う検証を行う。記録だけの変更ではリンク・書式・参照関係を確認し、意味のない全テストの繰り返しを避ける。必要なコマンドはREADMEを参照する。
- 同じチェックアウトの`.next`を使うサーバーは再ビルド前に停止する。ブラウザテストを複数同時実行するときは`--output`を分ける。
- CI成功、公開URLのHTTP成功、本番での操作確認、実機確認、PayPayの実送金を別々の事実として扱う。実送金を伴う検証は、明示的な承認がある場合だけ行う。

## 作業完了時に更新する

- 課題に進展があればBACKLOGの一覧・詳細を更新し、再現の解消、検証方法・結果、対象コミットまたはPR、残る制約を記録する。完了したIDは削除しない。
- 確認済み状態・仕様・次の着手順が変わったらSTATUSを更新する。挙動が変わったらREADMEも合わせる。
- 「実装した」「テストが通った」「本番で確認した」を区別する。課題を閉じるには、記載した完了条件を満たす証拠が必要。
- 新しい証跡は必要な範囲を`docs/verification/`に置き、個人情報・認証情報・実利用データを含めない。既存の検証記録は日付付きの履歴として残す。
